---
import '../../styles/Button.scss';

interface Props {
  variant?: 'primary' | 'secondary';
  class?: string;
  id?: string;
}

const { variant = 'primary', class: className, id } = Astro.props;
---

<button id={id} class={`button button-${variant} ${className || ''}`}>
  <slot />
</button>
